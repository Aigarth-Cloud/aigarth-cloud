{
  "name": "@aigarth/economy",
  "version": "0.1.0",
  "private": true,
  "description": "Aigarth Economy service — off-chain revenue splits, contributor shares, Qearn lock ledger, payouts, and bundle listings. The Intelligence Economy Protocol sub-layer approved by ADR 001.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "build": "tsc",
    "start": "node --env-file=.env dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"lint not configured yet\" && exit 0",
    "test": "echo \"no tests yet\" && exit 0",
    "smoke": "tsx src/services/splits.smoke.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx --env-file=.env src/db/migrate.ts",
    "worker:qearn-watcher": "tsx --env-file=.env src/workers/qearn-watcher.ts",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@aigarth/config": "workspace:*",
    "@aigarth/observability": "workspace:*",
    "@aigarth/utils": "workspace:*",
    "@fastify/cookie": "^9.4.0",
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/jwt": "^8.0.1",
    "drizzle-orm": "^0.42.0",
    "fastify": "^4.28.1",
    "nats": "^2.28.0",
    "pg": "^8.13.1",
    "pino": "^9.5.0",
    "pino-pretty": "^11.3.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "@types/pg": "^8.11.10",
    "drizzle-kit": "^0.30.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
