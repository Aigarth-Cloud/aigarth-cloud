/**
 * Seed script. Reserved for 19B.4 (public catalog starter data).
 * Empty in 19B.1 — public datasets will be created via the
 * authenticated API or imported from existing registries.
 */

import { closeDb } from "./index.js";

// eslint-disable-next-line no-console
console.log("[dataset] no seed data in 19B.1 — see 19B.4 for the public catalog starter.");
await closeDb();
