/**
 * Marketplace seed.
 *
 * Listings and auctions are user-created; no default seed rows needed for
 * Phase 6. The schema is fully ready for user creation.
 */

console.log("[marketplace] no seed rows — listings and auctions are user-created");
