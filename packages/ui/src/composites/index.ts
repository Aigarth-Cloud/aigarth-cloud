/**
 * Composite components — built from primitives, used across apps.
 * These are still part of the design system, but they bundle
 * multiple primitives or include app-specific behavior.
 *
 * (Currently empty — composites will be added as the design system grows.)
 */

export {};
