{
  "name": "@aigarth/oc-processor",
  "version": "0.1.0",
  "private": true,
  "description": "Aigarth-side Qubic Outsourced Computation processor (ADR 007). Receives signed 451/676 invocations from Qubic computors, maps them to Work Runtime work items, executes via the local OC engine, and returns a signed result.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "lint": "echo \"@aigarth/oc-processor lint not configured yet\" && exit 0",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@aigarth/config": "workspace:*",
    "zod": "~4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "@vitest/coverage-v8": "^2.1.2",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
